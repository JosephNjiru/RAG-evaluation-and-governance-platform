# ADR 0006: Cloud-aligned without mandatory cloud dependency

## Status

Accepted.

## Decision

The project includes Docker, API boundaries, CI checks and configuration separation without requiring a cloud platform.

## Reason

This keeps the design portable while avoiding unverified deployment claims.
